
# The human-readable product name of the package
title: Client Component Sampler Package

# The human-readable version of the package
version: 2.0.0

# A brief description of the package (rdoc formatting supported)
description: |
  This bundle contains an application that shows the default set of components
  in a full-screen gui application. Good for testing components and developing
  themes.

# System version requirement.
sys_version: '>= 2.0.0'

